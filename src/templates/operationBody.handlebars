{{{bodyMethodTsComments}}}{{methodName}}({{>operationParameters}}): Signal<ResponseWithError<{{{resultType}}}>> {
    return toSignal(this.{{responseMethodName}}(params, context).pipe(
      map((r: {{@root.responseClass}}<{{{resultType}}}>): ResponseWithError<{{{resultType}}}> => ({ data: r.body })),
      catchError((error) => of({data: null, error: error}))
    ), {
      injector: this._injector,
      initialValue: null
    });
  }