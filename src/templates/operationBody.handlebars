{{{bodyMethodTsComments}}}{{methodName}}({{>operationParameters}}): Signal<{{{resultType}}}|undefined> {
    return toSignal(this.{{responseMethodName}}(params, context).pipe(
      map((r: {{@root.responseClass}}<{{{resultType}}}>): {{{resultType}}} => r.body),
    ), {
      injector: this._injector
    });
  }